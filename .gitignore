# ── Dependencias ────────────────────────────────
# Las reinstala `npm ci` en cada deploy. No van al repo.
node_modules/

# ── Build y caché de Astro ──────────────────────
# Se regeneran solos. .astro es el que guardaba la URL vieja.
.astro/
dist/
.output/

# ── Variables de entorno / secretos ─────────────
.env
.env.local
.env.production

# ── Basura del sistema ──────────────────────────
.DS_Store
Thumbs.db

# ── Logs ────────────────────────────────────────
npm-debug.log*
yarn-error.log*
